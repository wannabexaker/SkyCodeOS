[package]
name = "skycode-memory"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
skycode-core = { path = "../skycode-core" }
rusqlite = { version = "0.31", features = ["bundled"] }
thiserror = "1"
serde = { version = "1", features = ["derive"] }
